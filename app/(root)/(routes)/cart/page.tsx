import CartClient from "./CartClient";

export const metadata = {
  title: "Shopping Cart - Elegant Essence",
  description: "Review your selected fragrances before checking out.",
};

export default function CartPage() {
  return <CartClient />;
}
